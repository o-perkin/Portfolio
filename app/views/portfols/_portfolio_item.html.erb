<div class="card">
  <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
  <p class="card-text">
    <span><%= link_to portfolio_item.title, portfol_show_path(portfolio_item) %></span>
    <%= portfolio_item.subtitle %>
  </p>
</div>